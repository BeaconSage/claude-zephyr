# Claude Zephyr 环境变量配置文件
# 复制此文件为 .env 并填入实际的认证令牌

# ============ 认证令牌配置 ============
# 主要API提供商的认证令牌 (必需)
AUTH_TOKEN_MAIN=sk-your-claude-auth-token-here

# 备用API提供商的认证令牌 (可选)
# AUTH_TOKEN_BACKUP=sk-another-auth-token-here

# 特殊用途的认证令牌 (可选)
# AUTH_TOKEN_SPECIAL=sk-special-auth-token-here

# ============ 其他配置 ============
# 服务监听端口 (可选，默认从config.toml读取)
# PROXY_PORT=8080

# Claude CLI 二进制文件路径 (可选)
# CLAUDE_BINARY_PATH=/path/to/claude

# ============ 配置说明 ============
# 1. 认证令牌格式:
#    - 以 sk- 开头的 Claude 认证令牌
#    - 从您的API提供商处获取
#
# 2. 环境变量命名:
#    - 使用描述性名称 (如 AUTH_TOKEN_MAIN)
#    - 避免地区代码，重点是提供商用途
#
# 3. 安全注意:
#    - .env 文件已在 .gitignore 中，不会被提交
#    - 每个认证令牌对应一个API提供商
#    - 健康检查时将使用 ANTHROPIC_AUTH_TOKEN 环境变量