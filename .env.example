# Claude Zephyr 环境变量配置文件
# 复制此文件为 .env 并填入实际的 AUTH_TOKEN

# 主要亚洲区域的认证令牌
AUTH_TOKEN_ASIA=sk-your-claude-auth-token-here

# 备用美国区域的认证令牌 (可选)
# AUTH_TOKEN_US=sk-another-auth-token-here

# 备用欧洲区域的认证令牌 (可选) 
# AUTH_TOKEN_EU=sk-yet-another-auth-token-here

# 服务监听端口
PROXY_PORT=8080

# Claude CLI 二进制文件路径 (可选，如果不在环境变量中)
# CLAUDE_BINARY_PATH=/path/to/claude/binary

# 注意事项:
# 1. AUTH_TOKEN 以 sk- 开头的 Claude 认证令牌
# 2. 可以配置多个不同区域/用途的 AUTH_TOKEN
# 3. 确保 .env 文件不要提交到版本控制系统
# 4. 每个 AUTH_TOKEN 可以对应多个 endpoints
# 5. 健康检查时将使用 ANTHROPIC_AUTH_TOKEN 环境变量